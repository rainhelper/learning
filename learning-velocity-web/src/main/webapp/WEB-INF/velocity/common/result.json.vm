#if("$!{callback}" != "")
if (typeof(${callback}) == "function") ${callback} (
#end
{
	"commonResult" : {
		"type" : "${commonResult.resultType}",
		"message" : "${commonResult.message}"
		#*
		"type" : "$!Escape.escapeJava(${commonResult.resultType})",
		"message" : "$!Escape.escapeJava(${commonResult.message})"
		*#
	}
}
#if("$!{callback}" != "")
);
#end
