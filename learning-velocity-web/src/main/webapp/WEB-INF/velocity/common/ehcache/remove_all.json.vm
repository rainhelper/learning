#if("$!{callback}" != "")
if (typeof(${callback}) == "function") ${callback} (
#end
{
	"ehcache" : "$!{cacheName}",
	"result" : {
		"message" : "${Escape.javascript(${message})}"
	}
}
#if("$!{callback}" != "")
);
#end
