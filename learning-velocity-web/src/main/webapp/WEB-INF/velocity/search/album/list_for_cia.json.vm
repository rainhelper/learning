{
"serviceid" : "music",
"sectionid" : "album_search",
"title" : "앨범 검색 결과 목록",
"list" :[
#foreach($item in $dto.albumResult.documentList)
	#if($foreach.index > 0)
		,
	#end
{
	"serviceid":"music",
	"sectionid":"album",
	"itemid" : "$!{item.album_id}",
	"title" : "$!{item.album_name}, $!{StringCutUtil.removeTagJSON($!{item.album_name})}",
	"link" : "/album/album.do?songId=$!{item.album_id}",
	"etc" : {
    	"artist" : [
    	{
					"artistId" : "$!{item.artist_id}",
					"artistname" : "${StringCutUtil.removeTagJSON($!{item.artist_name})}"
		}
    	],
    	"volume" : "$!{item.album_vol}",
    	"releasedate" : "$!{item.release_dttm}",
    	"thumbnail" : "$!{item.thumb}",
    	"titlesongid" : "$!{item.album_title_id}",
    	"titlesongname" : "$!{item.album_title_name}"    	
    }
}
#end
]
,
"paging" : {
        "page" : $!{command.pageNo},
        "count_per_page" : $!{dto.ph.onePageCnt},
#set($page_count = $MathTool.ceil($MathTool.div($!{dto.ph.totalCnt},$!{dto.ph.onePageCnt})))
        "page_count" : $page_count,
        "count" : $!{dto.ph.totalCnt}
    }
}
