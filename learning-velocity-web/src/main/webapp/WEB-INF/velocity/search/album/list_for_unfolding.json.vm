#if("$!{callback}" != "")
if (typeof(${callback}) == "function") ${callback} (
#end
{
	"albumList" : [
#foreach(${album} in ${dto.albumResult.documentList})
	#if(${foreach.index} > 0) , #end
	${JsonTool.toJson(${album.album})}
#end
	]
}
#if("$!{callback}" != "")
);
#end

