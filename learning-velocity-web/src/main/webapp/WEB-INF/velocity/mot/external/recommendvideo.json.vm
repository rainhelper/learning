{
"serviceid" : "music",
"sectionid" : "recommend_video",
"title" : "뮤직비디오 운영목록",
"list" :[
#foreach( $video in $vclipList )
		{
	        "serviceid": "music",
	        "sectionid": "video",
	        "itemid" : "$!{video.vclipId}",
	        "title" : "$!{EscapeUtil.escapeJsonString($!{video.title})}",
	        "link" : "http://music.daum.net/artist/vclip/detail?vclip_id=$!{video.vclipId}",
	        "image" : "$!{video.mainImage.filePath}",
	        "etc" : {
	        	"artist" : [
					#foreach($artist in $video.keyArtistList)
						{
						"artistdetailid" : "$!{artist.artistId}",
						"artistname" : "$!{EscapeUtil.escapeJsonString($!{artist.name})}"
						}#if($velocityHasNext),#end
					#end
		    	],
	        	"songid" : "$!{video.song.songId}",
	        	"songname" : "$!{EscapeUtil.escapeJsonString($!{video.song.title})}"
	        }
	    }#if($velocityHasNext),#end
#end
]
}